#!/bin/bash
# Amass - In-depth DNS enumeration and network mapping
# Options:
# enum : Perform enumeration
# -d : Domain to enumerate
# -o : Output file

# Usage: amass enum -d <target_domain> -o <output_file>

# Example script
amass enum -d example.com -o amass_output.txt
