#!/bin/bash
# Assetfinder - Find related domains and subdomains
# Options:
# --subs-only : Only find subdomains

# Usage: assetfinder --subs-only <target_domain>

# Example script
assetfinder --subs-only example.com > assetfinder_output.txt
