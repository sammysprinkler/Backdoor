#!/bin/bash
# Sublist3r - Fast subdomains enumeration tool
# Options:
# -d : Domain to enumerate
# -o : Output file to write results

# Usage: sublist3r -d <target_domain> -o <output_file>

# Example script
sublist3r -d example.com -o subdomains_output.txt
